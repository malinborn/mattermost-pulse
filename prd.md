PRD: Экспортер реакций из Mattermost в YAML

1. Цель

Сделать утилиту, которая по URL/ID поста в Mattermost забирает реакции только с первого (root) поста и отдает результат в YAML-формате:

emoji_name:
	•	user@dodobrands.io

Пример входного URL:
https://dodobrands.loop.ru/dodo-brands/pl/47jmpzgytig5pqwywh9gi3gqhe

2. Сценарий использования
	1.	Пользователь открывает веб-приложение (Streamlit).
	2.	Вводит:
	•	URL сервера Mattermost (если нужно);
	•	личный токен;
	•	ссылку на пост или ID поста.
	3.	Нажимает кнопку.
	4.	Приложение:
	•	определяет ID поста;
	•	по API получает реакции только для этого поста;
	•	по user_id вытаскивает email (fallback — username);
	•	собирает структуру emoji → список уникальных e-mail.
	5.	Пользователь видит YAML и может скачать файл.

3. Функциональные требования (MVP)
	•	Поля ввода: сервер (опционально), токен, ID/URL поста.
	•	Авторизация по личному токену пользователя.
	•	Вызов API Mattermost:
	•	получить реакции для одного указанного поста;
	•	получить данные пользователей для маппинга user_id → email.
	•	Агрегация:
	•	ключ — emoji_name;
	•	значение — список уникальных email/username.
	•	Вывод:
	•	YAML-превью на странице;
	•	кнопка «Скачать YAML».
	•	Ошибки:
	•	невалидный токен, недоступен пост, нет e-mail.

4. Нефункциональные требования
	•	Язык: Python 3.x
	•	UI: Streamlit
	•	HTTP: requests
	•	YAML: pyyaml (или аналог)
	•	Данные (токен, ответы API) не сохраняются на диск.

5. Реализованные дополнительные функции

	5.1 Режим выборочной обработки эмодзи
	
	**Решение по UI:** Использованы вкладки (tabs) в Streamlit для разделения двух режимов работы:
	
	•	Вкладка "Все эмодзи": Классический режим - выгрузка всех реакций с поста одной кнопкой.
	•	Вкладка "Выборочно": Новый режим с пошаговым процессом:
		1. Пользователь нажимает кнопку "Загрузить список эмодзи"
		2. Система получает все реакции и извлекает список уникальных эмодзи
		3. Пользователь выбирает нужные эмодзи через multiselect (по умолчанию выбраны все)
		4. Пользователь нажимает "Получить реакции по выбранным эмодзи"
		5. Система обрабатывает только выбранные эмодзи
	
	**Технические детали:**
	•	Добавлена функция `get_unique_emojis()` для извлечения списка уникальных эмодзи
	•	Функция `process_reactions()` теперь принимает опциональный параметр `emoji_filter`
	•	Использован `st.session_state` для хранения загруженных реакций между шагами
	•	Интерфейс разделен на вкладки для лучшей организации и UX

	5.2 Режим выгрузки треда (объединяет и расширяет базовый функционал)
	
	**Функциональность:** Основной режим работы для сбора реакций. Поддерживает как одиночный пост, так и полный тред с replies.
	
	**Реализация:**
	•	Вкладка "📥 Выгрузить тред": Универсальный режим для работы с постами и тредами
	•	Checkbox "Включить реакции из replies":
		- Если выключено - собираются реакции только с root поста (базовый функционал)
		- Если включено - собираются реакции со всех постов в треде с раздельным выводом
	•	Раздельное отображение результатов (при включенных replies):
		- Блок "📌 Root пост" - реакции только с основного поста
		- Блок "💬 Replies" - реакции со всех ответов в треде
		- Для каждого блока: количество реакций, уникальных эмодзи и JSON с данными
	•	Отображение статистики треда:
		- Количество постов в треде
		- Раздельная статистика по root и replies
	
	**API функции:**
	•	`get_thread_posts()` - получает все посты в треде через endpoint `/api/v4/posts/{post_id}/thread`
	•	`get_thread_reactions()` - собирает и агрегирует реакции со всех постов треда
	•	`get_thread_reactions_separated()` - собирает реакции раздельно для root и replies
	•	Автоматическая обработка edge cases (пост без replies, большие треды)
	
	**Workflow:**
	1. Пользователь вводит URL/ID поста
	2. Выбирает опцию включения/исключения replies (галка)
	3. Система получает структуру треда
	4. Если replies включены:
		- Собирает реакции раздельно (root + replies)
		- Показывает два отдельных блока с результатами
	5. Если replies выключены:
		- Собирает реакции только с root поста
		- Показывает единый блок с результатами

	5.3 Режим выгрузки канала с аналитикой реакций
	
	**Функциональность:** Выгрузка и анализ всех постов из канала Mattermost за заданный период с подробной статистикой по категориям статусов.
	
	**Реализация:**
	•	Вкладка "📊 Выгрузить канал": Режим для аналитики активности канала
	•	Входные параметры:
		- ID или URL канала Mattermost
		- Диапазон дат (по умолчанию: сегодняшний день)
		- Checkbox "Включить сообщения из тредов" (по умолчанию: выключен)
			* Если выключен - анализируются только root посты
			* Если включен - для каждого root поста загружаются реакции из треда и объединяются
	•	Автоматический анализ эмодзи:
		- Система выгружает все посты за указанный период
		- Фильтрует системные сообщения (присоединение/выход из канала)
		- Определяет уникальные эмодзи, используемые в реакциях на эти посты
		- Добавляет дефолтные эмодзи для категорий:
			* Done: :leaves:, :ice_cube:, :ballot_box_with_check:
			* In Progress: :hammer_and_wrench:
			* Control: :loading:, :eyes:
	•	Система категоризации эмодзи:
		- Три категории статусов: Done, In Progress, Control
		- Каждая категория в отдельном expander с multiselect
		- Пользователь распределяет эмодзи по категориям
		- Дефолтные эмодзи автоматически распределены по категориям
		- Умная фильтрация доступных эмодзи:
			* Done: доступны все эмодзи кроме дефолтных для In Progress и Control
			* In Progress: доступны эмодзи кроме выбранных в Done и дефолтных для Control
			* Control: доступны эмодзи кроме выбранных в Done и In Progress
			* Гарантирует отсутствие пересечений между категориями
			* Дефолтные значения последующих категорий резервируются для них
	•	Объединенная статистика (metrics в одной строке):
		- Всего постов
		- ✅ Done - количество постов
		- 🔧 In Progress - количество постов
		- 👁️ Control - количество постов
		- 📭 Нет реакций - количество постов
	•	Детальные списки постов по категориям:
		- Каждая категория в отдельном expander
		- Под заголовком показаны эмодзи категории
		- Список постов с деталями (автор, дата, текст, ссылка)
		- Посты уникальны (если пост имеет несколько эмодзи из категории, показывается один раз)
	•	Посты без реакций:
		- Показываются всегда в отдельном expander (expanded=False)
		- Включают кликабельные ссылки на посты
	•	Отображение деталей постов:
		- Автор поста (user_id)
		- Дата и время публикации
		- Текст сообщения (preview до 200 символов)
		- Кликабельная прямая ссылка на пост в Mattermost
	
	**API функции:**
	•	`get_channel_posts(channel_id, start_date, end_date)` - получает все посты канала за период с пагинацией
	•	`filter_system_messages(posts)` - фильтрует системные сообщения
	•	`filter_root_posts_only(posts)` - фильтрует только root посты (без replies)
	•	`enrich_posts_with_thread_reactions(server_url, token, posts)` - обогащает root посты реакциями из их тредов
	•	`get_posts_without_reactions(posts)` - фильтрует посты без реакций
	•	`get_posts_by_emoji(posts, emoji_name)` - фильтрует посты с конкретной реакцией
	•	`analyze_channel_emojis(posts)` - извлекает уникальные эмодзи + добавляет дефолтные
	•	`get_channel_info(server_url, token, channel_id)` - получает информацию о канале
	•	`get_team_info(server_url, token, team_id)` - получает информацию о команде
	•	`generate_post_link(server_url, team_name, post_id)` - генерирует ссылку на пост
	
	**Workflow:**
	1. Пользователь вводит ID/URL канала
	2. Выбирает диапазон дат (по умолчанию - сегодня)
	3. Устанавливает/снимает галку "Включить сообщения из тредов"
	4. Нажимает "Загрузить посты и проанализировать эмодзи"
	5. Система:
		- Получает все посты за период
		- Фильтрует системные сообщения
		- Фильтрует только root посты
		- Если включена галка - обогащает реакциями из тредов
		- Анализирует используемые эмодзи
		- Получает информацию о team для генерации ссылок
	6. Пользователь распределяет эмодзи по категориям (Done, In Progress, Control)
	7. Нажимает "Показать статистику"
	8. Система отображает:
		- Метрики по категориям (5 метрик в одной строке)
		- Детальные списки постов по каждой категории с кликабельными ссылками
		- Посты без реакций в отдельном expander

6. Будущее развитие
	•	Экспорт в YAML-файл с возможностью скачивания
	•	Дополнительные фильтры (по пользователям и т.п.)
	•	Возможность выборочной фильтрации эмодзи в режиме треда
	•	История выгрузок и сохранение настроек
	•	Расширенные графики с поддержкой множественных дней для режима "Выгрузить канал"
	•	Экспорт статистики канала в CSV/Excel