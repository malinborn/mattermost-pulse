# План разработки: Экспортер реакций Mattermost

## Этап 1: Настройка проекта
- Создать структуру проекта
- Настроить зависимости (`requirements.txt`: streamlit, requests, pyyaml)
- Создать основной файл приложения

## Этап 2: Базовый UI (Streamlit)
- Создать форму ввода:
  - Поле для URL сервера Mattermost
  - Поле для личного токена
  - Поле для URL/ID поста
- Добавить кнопку запуска

## Этап 3: Интеграция с Mattermost API
- Реализовать функцию парсинга post ID из URL
- Реализовать получение реакций для поста через API
- Реализовать получение данных пользователей (user_id → email/username)
- Добавить обработку ошибок (невалидный токен, недоступный пост)

## Этап 4: Обработка данных и формирование YAML
- Агрегировать реакции: emoji_name → список уникальных email
- Сформировать YAML-структуру
- Добавить превью YAML на странице
- Добавить кнопку скачивания файла

## Этап 5: Тестирование и доработка
- Протестировать с реальными данными Mattermost
- Проверить обработку edge cases
- Добавить README с инструкциями по запуску

## Этап 6: Функция обработки только выбранных эмодзи ✅ 
- ✅ Добавлена функция `get_unique_emojis()` для получения списка уникальных эмодзи
- ✅ Модифицирована функция `process_reactions()` с поддержкой фильтрации по эмодзи
- ✅ Интерфейс разделен на две вкладки:
  - Вкладка "Все эмодзи" - классический режим выгрузки всех реакций
  - Вкладка "Выборочно" - новый режим с пошаговым выбором эмодзи через multiselect
- ✅ Решение по UI зафиксировано в prd.md
- ✅ Код протестирован, приложение запускается корректно

## Этап 7: Сбор реакций из треда (всех сообщений) ✅
- ✅ Добавлена вкладка "📥 Выгрузить тред" (заменила первую вкладку "Все эмодзи")
- ✅ Реализована функция `get_thread_posts()` для получения всех постов в треде:
  - Использован endpoint `/api/v4/posts/{post_id}/thread`
  - Возвращает структуру с order и posts
- ✅ Реализована функция `get_thread_reactions()` для сбора реакций:
  - Собирает реакции с root поста и всех replies
  - Поддержка опционального параметра `include_replies`
  - Автоматическая агрегация всех реакций
- ✅ Реализована функция `get_thread_reactions_separated()`:
  - Собирает реакции раздельно для root и replies
  - Возвращает dict с ключами 'root' и 'replies'
- ✅ UI для вкладки с раздельным выводом:
  - Checkbox "Включить реакции из replies" (по умолчанию включено)
  - При включенном checkbox: два отдельных блока "📌 Root пост" и "💬 Replies"
  - При выключенном: только реакции с root поста
  - Отображение статистики: количество постов, реакций и эмодзи для каждого блока
- ✅ Обработка edge cases:
  - Пост без replies обрабатывается корректно
  - Автоматическая обработка всех постов через API
- ✅ Упрощена структура интерфейса: удалена избыточная вкладка "Все эмодзи"
- ✅ Обновлен prd.md с полным описанием нового функционала
- ✅ Код протестирован, приложение запускается корректно

## Этап 8: Backend API и UI загрузки для аналитики канала ✅
- ✅ Реализованы API функции для работы с каналами в `mattermost_api.py`:
  - `get_channel_posts(channel_id, start_date, end_date)` - выгрузка постов канала за период
    * Использовать endpoint `/api/v4/channels/{channel_id}/posts`
    * Поддержка пагинации для больших каналов (обработка параметров `page` и `per_page`)
    * Фильтрация по датам (конвертация дат в timestamp для API)
    * Возвращать список постов с полной информацией (автор, текст, timestamp, reactions)
  - `analyze_channel_emojis(posts)` - анализ эмодзи на всех постах
    * Собрать уникальные эмодзи из всех реакций на постах
    * Вернуть список найденных эмодзи (sorted)
    * Объединить с дефолтными эмодзи: :ballot_box_with_check:, :leaves:, :ice_cube:, :hammer_and_wrench:
  - `get_posts_without_reactions(posts)` - фильтр постов без реакций
    * Вернуть список постов, у которых `reactions` пустой или отсутствует
  - `get_posts_by_emoji(posts, emoji_name)` - фильтр постов с конкретной реакцией
    * Вернуть список постов, у которых есть указанная реакция
    * Включить количество данной реакции для каждого поста
- Добавить вспомогательные функции:
  - `parse_channel_id_from_url(url)` - извлечение ID канала из URL Mattermost
  - `format_post_preview(post_text, max_length=100)` - форматирование превью текста
  - `generate_post_link(server_url, team_id, post_id)` - генерация прямой ссылки на пост
- Обработка ошибок API:
  - Невалидный ID канала (404)
  - Канал без прав доступа (403)
  - Проблемы с токеном (401)
  - Timeout при большом количестве постов
- Добавить новую вкладку "📊 Выгрузить канал" в `app.py`
- Реализовать UI для ввода параметров:
  - Поле для ID/URL канала (text_input с placeholder)
  - Date picker для диапазона дат:
    * Начальная дата (по умолчанию - сегодня)
    * Конечная дата (по умолчанию - сегодня)
  - Валидация введенных данных перед отправкой
- Кнопка "Загрузить посты и проанализировать эмодзи":
  - При нажатии показать spinner с прогрессом
  - Выгрузить посты через `get_channel_posts()`
  - Проанализировать эмодзи через `analyze_channel_emojis()`
  - Сохранить результаты в `st.session_state`
- После успешной загрузки показать:
  - Информационный блок с количеством найденных постов
  - Multiselect для выбора эмодзи:
    * Список найденных эмодзи + дефолтные
    * По умолчанию ничего не выбрано (пользователь выбирает сам)
    * Добавить эмодзи иконки для наглядности
  - Checkbox "Показать посты без реакций"
  - Кнопка "Показать статистику" (активна только после выбора хотя бы одного фильтра)
- Использовать `st.session_state` для хранения:
  - `channel_posts` - загруженные посты
  - `found_emojis` - найденные эмодзи
  - `selected_emojis` - выбранные пользователем эмодзи
  - `show_no_reactions` - флаг отображения постов без реакций
- Обработка edge cases:
  - Пустой канал или период без постов (показать warning)
  - Большое количество постов (показать progress bar)
  - Ошибки API (показать error message с деталями)
- Тестирование:
  - Протестировать backend с реальным каналом и различными периодами
  - Проверить корректность пагинации
  - Убедиться в правильной обработке постов без реакций
  - Протестировать UI и взаимодействие с пользователем

### Доработки этапа 8:
- ✅ Добавлена галка "Включить сообщения из тредов":
  - По умолчанию выключена (анализируются только root посты)
  - При включении для каждого root поста загружаются реакции из треда
  - Реализована функция `enrich_posts_with_thread_reactions()`
  - Показывается прогресс загрузки тредов
- ✅ Фильтрация системных сообщений:
  - Добавлена функция `filter_system_messages(posts)` 
  - Автоматически фильтрует сообщения о присоединении/выходе из канала
  - Применяется сразу после загрузки постов
- ✅ Кликабельные ссылки на посты:
  - Добавлены функции `get_channel_info()`, `get_team_info()`, `generate_post_link()`
  - Каждый пост имеет кликабельную ссылку для перехода в Mattermost
  - Ссылки генерируются с использованием реального team name
- ✅ Система категоризации эмодзи:
  - Три категории: Done, In Progress, Control
  - Каждая категория в отдельном expander с multiselect
  - Дефолтные эмодзи автоматически распределены:
    * Done: :leaves:, :ice_cube:, :ballot_box_with_check:
    * In Progress: :hammer_and_wrench:
    * Control: :loading:, :eyes:
  - Пользователь может добавлять/убирать эмодзи в категории
- ✅ Объединенная статистика:
  - Убрана отдельная таблица сводки
  - 5 метрик в одной строке: Всего | Done | In Progress | Control | Нет реакций
  - Компактное и наглядное отображение
- ✅ Посты без реакций показываются всегда:
  - Убран checkbox "Показать посты без реакций"
  - Посты без реакций всегда в отдельном expander (expanded=False)
  - Включают кликабельные ссылки
- ✅ Группировка постов по категориям:
  - Посты отображаются по категориям, а не по эмодзи
  - Для каждой категории показываются ее эмодзи
  - Посты уникальны внутри категории
  - Порядок категорий: Done → In Progress → Control → Нет реакций
- ✅ Дополнительные функции:
  - `filter_root_posts_only(posts)` - фильтрация только root постов
  - Обработка пагинации для больших каналов
  - Обработка ошибок API (404, 403, 401, timeout)
  - Информационные сообщения о прогрессе обработки

## Этап 9: UI отображения статистики и результатов
- Реализовать отображение статистики после нажатия "Показать статистику":
  - Блок общей статистики (metrics):
    * Общее количество постов за период
    * Количество постов с реакциями
    * Количество постов без реакций
  - Expandable секции для каждой выбранной категории эмодзи:
    * Заголовок с эмодзи и счетчиком постов
    * Список постов внутри expander:
      - Автор (username или email)
      - Превью текста (первые 100 символов)
      - Прямая ссылка на пост (clickable)
      - Дата и время публикации
  - Секция "Посты без реакций" (если checkbox активен):
    * Счетчик постов
    * Аналогичный список постов с деталями
- Визуализация - график распределения реакций:
  - Bar chart с количеством постов по категориям
  - По оси X: выбранные эмодзи + "без реакций" (если выбрано)
  - По оси Y: количество постов
  - Использовать `st.bar_chart()` или библиотеку plotly для интерактивности
  - Добавить цветовое кодирование для лучшей читаемости
- Дополнительные функции отображения:
  - Возможность скопировать список постов
  - Сортировка постов (по дате, по количеству реакций)
  - Ограничение отображения (показать первые 50, кнопка "Показать еще")
- Тестирование UI:
  - Проверить читаемость при большом количестве постов
  - Убедиться в корректности всех ссылок
  - Протестировать различные комбинации фильтров
  - Проверить корректность графика
- Обновить документацию:
  - Добавить в README описание функционала "Выгрузить канал"
  - Добавить скриншоты или примеры использования
  - Описать формат входных данных (ID канала, даты)